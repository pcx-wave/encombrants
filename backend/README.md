# App Encombrants (Template Flask)

## Lancer l'app en local :

```bash
python3 -m venv venv
source venv/bin/activate  # ou .\venv\Scripts\activate sur Windows
pip install -r requirements.txt
python app.py
```
